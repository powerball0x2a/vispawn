# Vispawn 依赖
# 推荐使用 pyproject.toml 安装: pip install -e .

# 核心依赖
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
httpx>=0.25.0
python-dotenv>=1.0.0
pydantic>=2.0.0
python-multipart>=0.0.6

# 开发依赖
pytest>=7.0.0
pytest-asyncio>=0.21.0
